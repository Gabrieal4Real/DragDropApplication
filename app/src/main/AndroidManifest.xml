<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.danieljayarajan.draganddropapplication">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.DragAndDropApplication">
        <activity android:name=".activities.baseActivities.SplashScreenActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".activities.loginActivities.LoginActivity" />
        <activity android:name=".activities.loginActivities.SignUpActivity" />
        <activity android:name=".activities.dragDropActivities.DragDropActivity" />
    </application>
</manifest>